project_name = "__PROJECT_NAME__"
random_seed = 0

[logging]
level = "INFO"

# Merge one source table per real source and one product table per stage product
# from the selected storage assets. Values only: never put credentials or
# implementation class paths in this file.

[quality]
critical_rules = ["__CRITICAL_CONTRACT_OR_INTEGRATION_RULE__"]
row_rejection_policy = "quarantine"
quarantine_product = "__QUARANTINE_PRODUCT_NAME__"
