# SECURITY Skeleton

Copy the block below to `SECURITY.md`, then rework every line. The
reporting channel must be one a named person actually monitors — verify
with the user before committing. Check the fetched docs for the
platform's private vulnerability reporting feature and prefer it when
available, so reports never start in a public issue.

```markdown
# Security Policy

## Reporting a vulnerability

<The private channel: the platform's private reporting form, or a
monitored address. Never a public issue.>

You will receive a response within <realistic commitment> from <owner>.

## Supported versions

<Which versions receive security fixes — delete if the project simply
tracks the latest release.>
```
