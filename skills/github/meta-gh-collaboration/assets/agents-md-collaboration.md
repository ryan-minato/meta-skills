## Collaboration

- Commits: <format — spec name and version if one was adopted; allowed
  types and scope meanings; subject length limit>. Enforced by <hook,
  check, or review>.
- Branches: <naming pattern>; branch from <default branch>; never commit
  directly to <default branch>.
- Pull requests: <draft usage>; keep a PR to <size guideline>; fill the
  PR template; link the issue it closes.
- Review: <who reviews what>; approval requires <criteria>; respond to
  review comments within <expectation>.
- Agent-authored changes: <how agents mark their PRs and commits>.
- Issues: <when an issue is required before starting work>; bug reports
  and feature requests use the templates under `.github/ISSUE_TEMPLATE/`.
