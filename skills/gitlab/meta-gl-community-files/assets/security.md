# SECURITY Skeleton

Copy the block below to `SECURITY.md`, then rework every line. The
reporting channel must be one a named person actually monitors — verify
with the user before committing. Check live whether this instance and
tier offer a private vulnerability-reporting feature and prefer it when
available, so reports never start in a public issue; confidential issues
are the common fallback.

```markdown
# Security Policy

## Reporting a vulnerability

<The private channel: the instance's private reporting feature, a
confidential issue, or a monitored address. Never a public issue.>

You will receive a response within <realistic commitment> from <owner>.

## Supported versions

<Which versions receive security fixes — delete if the project simply
tracks the latest release.>
```
